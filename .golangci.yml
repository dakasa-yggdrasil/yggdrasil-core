version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - unused
    - ineffassign

issues:
  exclude-rules:
    - linters:
        - errcheck
      text: "Error return value of .*(Close|Rollback).*is not checked"
